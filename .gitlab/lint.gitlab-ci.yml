stages:
  - lint

lint_application:
  stage: lint
  image: node:latest
  tags:
    - linux
    - konsulent
    - docker
  script:
    - npm ci --cache .npm --no-optional --prefer-offline
    - npm run lint
    - npm run format:check
